# Scripts (starter folder)

This folder is for future backup automation scripts.

Planned examples:
- GitHub inventory export script
- GitHub org/team/settings export script
- App Center inventory/export helper script

For now, keep scripts simple and safe:
- do not hardcode tokens
- read secrets from environment variables
- write output files into a tracked `exports/` folder (to be added later)
